[project]
name = "work0-gravity-lab"
version = "0.1.0"
description = "Taichi GPU gravity swarm simulation (Work0)"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "taichi>=1.7.4",
]
